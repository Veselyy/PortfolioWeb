import { Stack } from '@mui/material';
import ThemeSwitcher from './navbar/ThemeSwitcher';

function Navbar() {
  return (
    <Stack>
      <ThemeSwitcher />
    </Stack>
  );
}

export default Navbar;
