export NVM_DIR="$HOME/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && . "$NVM_DIR/nvm.sh"

nvm use --silent >/dev/null 2>&1 || true

pnpm run format:check
pnpm run lint

if sh scripts/should-check-cv-sync.sh; then
  pnpm run cv:check
fi
